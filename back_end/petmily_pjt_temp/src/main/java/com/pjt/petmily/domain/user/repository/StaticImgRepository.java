package com.pjt.petmily.domain.user.repository;

import com.pjt.petmily.domain.user.entity.StaticImg;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StaticImgRepository extends JpaRepository<StaticImg, Long> {
}
